const db = require('../models')
const { scheduleItems } = db

 const scheduleControllers = {
   addScheduleItems: (req, res) => {
    const{ moduleIds, date } = req.body
    const exerciseIds =  
 }
 }
module.exports = scheduleControllers
